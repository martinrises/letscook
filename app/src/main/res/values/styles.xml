<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="detail_padding">
        <item name="android:paddingLeft">@dimen/detail_padding_left_right</item>
        <item name="android:paddingRight">@dimen/detail_padding_left_right</item>
        <item name="android:paddingTop">@dimen/detail_padding_top_bottom</item>
        <item name="android:paddingBottom">@dimen/detail_padding_top_bottom</item>
    </style>

    <style name="detail_margin">
        <item name="android:layout_marginLeft">10dip</item>
        <item name="android:layout_marginRight">10dip</item>
    </style>

    <style name="item_image">
        <item name="android:layout_width">120dip</item>
        <item name="android:layout_height">80dip</item>
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_marginBottom">@dimen/margin_recipe_item_top_bottom</item>
        <item name="android:layout_marginLeft">@dimen/margin_recipe_item_top_bottom</item>
        <item name="android:background">@color/bg_img_recipe</item>
    </style>

    <style name="view_shop_dimen">
        <item name="android:layout_height">19dip</item>
        <item name="android:layout_width">0px</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="menu_labels_style">
        <item name="android:background">@drawable/fab_label_background</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="tv_main_indicator">
        <item name="android:layout_width">0px</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">18dip</item>
    </style>

</resources>
